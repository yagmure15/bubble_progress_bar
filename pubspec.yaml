name: bubble_progress_bar
description: A premium, performance-optimized gradient progress bar with beautiful animated bubble particles.
version: 0.0.3
repository: https://github.com/yagmure15/bubble_progress_bar
homepage: https://github.com/yagmure15/bubble_progress_bar

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  # This section is empty but required for Flutter packages.
